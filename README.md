# WiFaire : Go to the stars ✨

  __How to use the file :__

Do :  pip install tkinter <br>
      pip install tk
      pip install wifi
      pip install os
      pip install base64
      pip install colorama
      <hr>

      
